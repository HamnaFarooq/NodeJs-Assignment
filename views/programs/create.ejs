@extends('layouts.layout')
@section('content')

<a href="{{'/programs','ProgramsController'}}" class="btn"> Go Back </a>

<div class="container-fluid">
    <div class="container">
        <h2>Add program</h2>
        <form action="{{'/programs','ProgramsController'}}" method="POST">
            @csrf

            <div class="form-group">
                <label for="ptitle">program Name:</label>
                <input type="text" class="form-control" name="ptitle" placeholder="Enter Program name" required pattern="[A-Z]{3}">
                <p>(3 letters only e.g: BSC, BIT)</p>
            </div>

            <div class="form-group">
                <label for="duration">duration:</label>
                <select class="form-control" name="duration">
                    <option selected>---- years ----</option>
                    <option value="0.5 year">6 months</option>
                    <option value="1 year">1 year</option>
                    <option value="2 years">2 years</option>
                    <option value="3 years">3  years</option>
                    <option value="4 years">4 years</option>
                    <option value="5 years">5 years</option>
                    <option value="6 years">6 years</option>
                    <option value="7 years">7 years</option>
                </select>
            </div>

            @if($errors->any())
            @foreach ($errors->all() as $error)
            <div class="alert alert-danger">
                {{$error}}
            </div>
            @endforeach
            @endif

            <button type="submit" class="btn btn-primary">Submit</button>

        </form>
    </div>
</div>

@endsection
