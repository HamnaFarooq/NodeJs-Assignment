<div class="content">
    <div class="title m-b-md">
        course
    </div>
    <div class="row">
        <a href="/course/create" class="btn btn-primary">Add another course</a>
    </div>

    <div class="row">
        <% for(var i=0; i < societies.length; i++) { const society = societies[i] %>

        <div class="col col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{$course->coursetitle}}</h5>
                    <h5 class="card-title">{{$course->crdhr}}</h5>
                    <a href="/course/{{$course->code}}" class="btn btn-primary">View</a>
                </div>
            </div>
        </div>

        <%}%

    </div>



</div>
