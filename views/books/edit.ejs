@extends('layouts.layout')
@section('content')

<a href="{{'/books','booksController'}}" class="btn"> Go Back </a>

    <div class="container-fluid">
      <div class="container">
        <h2>Edit book</h2>
        <form action="/books/{{$book->Isbn}}" method="POST">
            @csrf
            @method('patch')

            <div class="form-group">
              <label for="Title">Book Title:</label>
              <input type="text" class="form-control" name="Title" value="{{$book->Title}}" placeholder="Enter title" required>
            </div>

            <div class="form-group">
              <label for="Author">Author:</label>
              <input type="text" class="form-control" name="Author" value="{{$book->Author}}"  placeholder="Enter Author name" required>
            </div>

            <div class="form-group">
              <label for="Price">Price:</label>
              <input type="number" class="form-control" name="Price" value="{{$book->Price}}" placeholder="Enter Price" required>
            </div>

            <div class="form-group">
              <label for="Publisher">Publisher:</label>
              <input type="text" class="form-control" name="Publisher" value="{{$book->Publisher}}"  placeholder="Enter Publisher name" required>
            </div>

            <div class="form-group">
              <label for="copies">Copies:</label>
              <input type="number" class="form-control" name="copies" value="{{$book->copies}}"  placeholder="number of copies" required>
            </div>

          @if($errors->any())
            @foreach ($errors->all() as $error)
                <div class="alert alert-danger">
                    {{$error}}
                </div>
            @endforeach
          @endif

          <button type="submit" class="btn btn-primary">Update</button>

        </form>
        </div>
    	</div>

@endsection
