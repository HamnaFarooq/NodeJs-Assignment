@extends('layouts.layout')
@section('content')

<a href="{{'/sessions','sessionsController'}}" class="btn"> Go Back </a>

<div class="container-fluid">
    <div class="container">
        <h2>Add session</h2>
        <form action="{{'/sessions','sessionsController'}}" method="POST">
            @csrf

            <div class="form-group">
                <label for="ptitle">Session Name:</label>
                <input type="text" class="form-control" name="stitle" placeholder="Enter session name" required pattern="[A-Z][0-9]{2}">
                <p>3 to 4 letters only!  (e.g: S12, F12 )</p>
            </div>

            <div class="form-group">
                <label for="sdate">Start Month:</label>
                <select class="form-control" name="sdate">
                    <option value="">---- months ----</option>
                    <option value="January">January</option>
                    <option value="February">February</option>
                    <option value="March">March</option>
                    <option value="April">April</option>
                    <option value="May">May</option>
                    <option value="June">June</option>
                    <option value="July">July</option>
                    <option value="August">August</option>
                    <option value="September">September</option>
                    <option value="Octuber">Octuber</option>
                    <option value="November">November</option>
                    <option value="December">December</option>
                </select>
            </div>

            @if($errors->any())
            @foreach ($errors->all() as $error)
            <div class="alert alert-danger">
                {{$error}}
            </div>
            @endforeach
            @endif

            <button type="submit" class="btn btn-primary">Submit</button>

        </form>
    </div>
</div>

@endsection
