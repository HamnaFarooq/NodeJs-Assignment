<div class="content">
    <div class="title m-b-md">
        User
    </div>
    <div class="row">
        <a href="/user/create" class="btn btn-primary">Add another user</a>
    </div>

    <div class="row">
        <% for(var i=0; i < societies.length; i++) { const society = societies[i] %>

        <div class="col col-md-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{$user->id}}</h5>
                    <h5 class="card-title">{{$user->type->tname}}</h5>
                    <h5 class="card-title">{{$user->password}}</h5>
                    <a href="/user/{{$user->id}}/edit" class="btn btn-primary">Edit</a>
                    <a href="/books/{{$book->Isbn}}/delete" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>

        <%}%

    </div>
</div>
