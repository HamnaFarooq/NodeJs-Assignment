@extends('layouts.layout')
@section('content')

<a href="{{'/societies','societiesController'}}" class="btn"> Go Back </a>

    <div class="container-fluid">
      <div class="container">
        <h2>Add societie</h2>
        <form action="{{'/societies','societiesController'}}" method="POST">
            @csrf

          <div class="form-group">
            <label for="socname">Society Name:</label>
            <input type="text" class="form-control" name="socname" placeholder="Enter society name" required>
          </div>

          <div class="form-group">
            <label for="type">Society type:</label>
            <input type="text" class="form-control" name="type" placeholder="Enter society type" required>
          </div>

          @if($errors->any())
            @foreach ($errors->all() as $error)
                <div class="alert alert-danger">
                    {{$error}}
                </div>
            @endforeach
          @endif

          <button type="submit" class="btn btn-primary">Submit</button>

        </form>
        </div>
    	</div>

@endsection
