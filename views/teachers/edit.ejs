@extends('layouts.layout')
@section('content')

<a href="{{'/teachers','TeachersController'}}" class="btn"> Go Back </a>

    <div class="container-fluid">
      <div class="container">
        <h2>Edit Teacher</h2>
        <form action="/teachers/{{$teacher->TId}}" method="POST">
            @csrf
            @method('patch')

          <div class="form-group">
            <label for="Firstname">Teacher Name:</label>
            <input type="text" class="form-control" name="Firstname" value="{{$teacher->Firstname}}"  placeholder="Enter First name" required>
          </div>

          <div class="form-group">
            <label for="Lastname">Teacher Name:</label>
            <input type="text" class="form-control" name="Lastname" value="{{$teacher->Lastname}}" placeholder="Enter First name" required>
          </div>

          <div class="form-group">
            <label for="Designation">Designation:</label>
            <input type="text" class="form-control" name="Designation" value="{{$teacher->Designation}}"  placeholder="Enter Designation" required>
          </div>

          <div class="form-group">
              <label for="Experience">Eperience:</label>
              <select class="form-control" name="Experience" value="{{$teacher->Teachername}}" >
                  <option value="">---- Years ----</option>
                  <option value="1 year"> 1 year </option>
                    @php
                        for ($i=2; $i<=100; $i++)
                        {
                            @endphp
                            <option value="<?php echo $i;?> years"><?php echo $i;?> years </option>
                            @php
                        }
                    @endphp
              </select>
          </div>

          @if($errors->any())
            @foreach ($errors->all() as $error)
                <div class="alert alert-danger">
                    {{$error}}
                </div>
            @endforeach
          @endif

          <button type="submit" class="btn btn-primary">Update</button>

        </form>
        </div>
    	</div>

@endsection
